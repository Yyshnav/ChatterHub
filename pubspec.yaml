name: chatapp
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
# Compatible with Dart SDK 3.x

dependencies:
  flutter:
    sdk: flutter

  # Firebase plugins compatible with Dart SDK 3.x.
  cupertino_icons: ^1.0.8
  firebase_core: ^3.4.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.5.0
  provider: ^6.1.2
  emoji_picker_flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Lints to follow recommended coding standards
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
